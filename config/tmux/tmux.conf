# -------
# plugins
# -------

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-sensible'

# ----------
# tmux-continuum
# ----------

set -g @continuum-restore 'on'

# load plugins
run ~/.tmux/plugins/tpm/tpm

# -------
# general
# -------

set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g history-limit 50000
set -g escape-time 0
set -g focus-events on
set -g set-clipboard on
set -g allow-passthrough on
setw -g aggressive-resize on
set -g detach-on-destroy off

# enable hyperlinks
set -as terminal-features ",*:hyperlinks"

# -------
# prefix
# -------

set -g prefix C-Space
set -g prefix2 C-b
bind C-Space send-prefix

# ----------
# status bar
# ----------

set -g status-position top
set -g status-interval 5
set -g status-left-length 30
set -g status-right-length 100
set -g window-status-separator ""

# ------
# theme
# ------

set -g status-style "bg=default,fg=default"
set -g status-left "#[fg=black,bg=blue,bold] #S #[bg=default] "
set -g status-right "#[fg=blue]#{?client_prefix,PREFIX ,}#[fg=brightblack]cwd: #(prettypath -compact \"#{pane_current_path}\") #h "
set -g window-status-format "#[fg=brightblack] #I:#{?automatic-rename,#(prettypath -max 3 -compact \"#{pane_current_path}\"),#W} "
set -g window-status-current-format "#[fg=blue,bold] #I:#{?automatic-rename,#(basename \"#{pane_current_path}\"),#W} "
set -g pane-border-style "fg=brightblack"
set -g pane-active-border-style "fg=blue"
set -g message-style "bg=default,fg=blue"
set -g message-command-style "bg=default,fg=blue"
set -g mode-style "bg=blue,fg=black"
setw -g clock-mode-colour blue

# -----------
# keybindings
# -----------

# vi mode for copy
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# reload config
unbind r
bind r source-file ~/.config/tmux/tmux.conf

# pane splits (open in current path)
bind h split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"
bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind x kill-pane

# pane navigation
bind -n C-M-Left select-pane -L
bind -n C-M-Right select-pane -R
bind -n C-M-Up select-pane -U
bind -n C-M-Down select-pane -D

# pane navigation (vim-style)
bind-key -r C-h select-pane -L
bind-key -r C-j select-pane -D
bind-key -r C-k select-pane -U
bind-key -r C-l select-pane -R

# pane resize
bind -n C-M-S-Left resize-pane -L 5
bind -n C-M-S-Down resize-pane -D 5
bind -n C-M-S-Up resize-pane -U 5
bind -n C-M-S-Right resize-pane -R 5

# window navigation
bind c new-window -c "#{pane_current_path}"
bind k kill-window
bind C-r setw automatic-rename on

bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

bind -n M-Left select-window -t -1
bind -n M-Right select-window -t +1

# window navigation (ctrl-style)
bind-key -r C-y previous-window
bind-key -r C-u next-window

# session controls
bind R command-prompt -I "#S" "rename-session -- '%%'"
bind C new-session -c "#{pane_current_path}"
bind K kill-session
bind P switch-client -p
bind N switch-client -n

bind -n M-Up switch-client -p
bind -n M-Down switch-client -n
